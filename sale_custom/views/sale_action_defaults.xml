<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- The counter and the desks work the whole lab's queue, not their own quotations:
         Odoo's default "My Quotations" filter hid everybody else's work on every open of
         the screen and had to be removed by hand each time. (client, 2026-09-24) -->
    <record id="sale.action_quotations" model="ir.actions.act_window">
        <field name="context">{}</field>
    </record>
    <record id="sale.action_quotations_with_onboarding" model="ir.actions.act_window">
        <field name="context">{}</field>
    </record>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!--
            Fallback paper format for the label report.  Every template maintains a
            paper format of its own and passes it through the print wizard, so this
            one is only ever used if a report is rendered without a template.
        -->
        <record id="paperformat_epg_label_a4" model="report.paperformat">
            <field name="name">Labels: A4 (no margins)</field>
            <field name="default" eval="False"/>
            <field name="format">A4</field>
            <field name="page_height">0</field>
            <field name="page_width">0</field>
            <field name="orientation">Portrait</field>
            <field name="margin_top">0</field>
            <field name="margin_bottom">0</field>
            <field name="margin_left">0</field>
            <field name="margin_right">0</field>
            <field name="header_line" eval="False"/>
            <field name="header_spacing">0</field>
            <field name="dpi">96</field>
            <field name="disable_shrinking" eval="True"/>
        </record>

    </data>
</odoo>
